### Local environment overrides (example) - DO NOT COMMIT SECRETS HERE IN PRODUCTION
# Copy this to `.env.local` and set real values locally (this file is committed only as an example)

APP_ENV=dev
# Set a strong secret locally: e.g. generate with `openssl rand -hex 32` or `php -r "echo bin2hex(random_bytes(32));"`.
APP_SECRET=REPLACE_WITH_SECURE_SECRET
